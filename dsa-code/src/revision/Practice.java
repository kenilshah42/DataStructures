package revision;import java.math.BigDecimal;import java.math.RoundingMode;import java.util.ArrayList;import java.util.List;import java.util.TimeZone;public class Practice {    public static void main(String args[]){        BigDecimal amount = BigDecimal.valueOf(0.00);        System.out.println("Amount - "+ amount);        System.out.println(amount.setScale(2, RoundingMode.HALF_EVEN));        int[] array = {4,3,2,7,8,2,3,1};        TimeZone tz = TimeZone.getTimeZone("Asia/Calcutta");        System.out.println("Zone id - >" + tz.getID());        System.out.println("Zone name - >" + tz.getDisplayName());        System.out.println("Zone id - >" + tz.toZoneId());        //System.out.println("missing element -> "+ missingElement(array).toString());    }    static List<Integer> missingElement(int[] array){        int i = 0;        List<Integer> ls = new ArrayList<>();        while(i < array.length){            int correctIndex = array[i]-1;            if(i != correctIndex){                if(array[i] != array[correctIndex]){                    int temp = array[i];                    array[i] = array[correctIndex];                    array[correctIndex] = temp;                }else{                    ls.add(array[i]);                    i++;                }            }else{                i++;            }        }        return ls;    }}