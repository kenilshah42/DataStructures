package org.example.revised;public class FindPeakElement {    public static void main(String args[]) {        int[] array = {0, 1, 3, 1, 0};        System.out.println(findPeak(0, array.length - 1, array));    }    static int findPeak(int start, int end, int[] array) {        if (start == end) {            return start;        }        int mid = start + (end - start) / 2;        if (array[mid] > array[mid + 1]) {            return findPeak(start, mid, array); //descending order        } else {            return findPeak(mid + 1, end, array); //ascending order        }    }}