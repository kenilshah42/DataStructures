package org.example.revised;import java.util.Arrays;public class FindMissingElement {    public static void main(String args[]){        int[] array = {3,0,1};        int maxRange = 3; // [0,n] -> [0,4] total 5 elements        System.out.println("Missing element -> "+ findMissingValue(array,maxRange));    }    static int findMissingValue(int[] array,int maxRange){        int i = 0;        while(i < array.length){            int correct = array[i];            if(correct != array.length && array[i] != array[correct]){                int temp = array[i];                array[i] = array[correct];                array[correct] = temp;            }else{                i++;            }        }        System.out.println("Current array -> "+ Arrays.toString(array));        for(int index = 0;index < array.length;index++){            if(index != array[index]){                return index;            }        }        return maxRange+1;    }}